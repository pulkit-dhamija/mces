	AREA RESET,CODE
	LDR R0,=LIST
	LDMIA R0,{R1-R10}
	MOV SP,#0x40000000
	STMIA SP,{R1-R10}
	MOV R1,#0x40000000; ADDRESS OF 1ST NUMBER
	MOV R2,#9; TOTAL COUNT
LOOPI MOV R3,R2;I=J
	MOV R4,R1
LOOPJ LDR R5,[R4];jTH ELEMENT
	LDR R6,[R4,#4];k+1TH ELEMENT
	CMP R5,R6
	BLS LESS
	MOV R7,R5;R7 IS TEMP
	MOV R5,R6
	MOV R6,R7
	STR R5,[R4]
	STR R6,[R4,#4]
LESS ADD R4,#4
	SUBS R3,#1
	BNE LOOPJ
	SUBS R2,#1
	BNE LOOPI
STOP B STOP
LIST DCD 2,7,4,5,11,18,3,15,8,0
	END